import { ProductAssessmentState } from './productAssessment/productAssessmentSlice';

export interface RootState {
  productAssessment: ProductAssessmentState;
  // Add other slice states here as needed
} 